#include <bits/stdc++.h>
#include <iostream>
#include <cstdio>

using namespace std;

int main()
{
    int t, n, m;
    int *a;
    cin >> t;
    int i = 0;
    while (i < t)
    {
        cin >> n >> m;
        a = new int[n];
        for (int j = 0; j < n; j++)
            cin >> a[j];
        
    }
    return 0;
}