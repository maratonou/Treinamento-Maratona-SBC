#include <stdio.h>
#include <stdlib.h>

#define forn(i, n) for(i = 0; i < n; i++)

int compare()
{
    
}

void solve()
{
    int n, k;

    scanf("%d %d", &n, &k);
    int *ratos = (int *)calloc(k, sizeof(int));

    int i;
    for (i = 0; i < k; i++)
}
 
int main() {

	int t = 1;
    scanf("%d", &t);
	while(t--) solve();
 	return 0;
}