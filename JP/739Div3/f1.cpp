#include "bits/stdc++.h"
#include <math.h>

 
using namespace std;
 
using ll = long long;
using ii = pair<int, int>;
using ull = unsigned long long;

void solve()
{
    int n, k;
    cin >> n >> k;
    vector<int> bits(10, 0);
    vector<int> k_numbers(k, 0);

}
 
int main() {
	ios_base :: sync_with_stdio(false);
	cin.tie(0);
	int t = 1;
	cin >> t;
	while(t--) solve();
 	return 0;
}